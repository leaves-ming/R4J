# Hybrid Retrieval Guide

Hybrid retrieval combines semantic matching and keyword matching.

It improves recall by keeping lexical precision and dense understanding in one pipeline.

This sample file is used by the quickstart ingestion flow.
